import 'package:flutter/material.dart';

class XPProgress extends StatelessWidget {
  const XPProgress({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('XP & Progress')),
      body: const Center(
        child: Text('XP & Progress Circle - Coming Soon!'),
      ),
    );
  }
}
