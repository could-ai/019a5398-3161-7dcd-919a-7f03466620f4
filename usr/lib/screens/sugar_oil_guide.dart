import 'package:flutter/material.dart';

class SugarOilGuide extends StatelessWidget {
  const SugarOilGuide({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Sugar & Oil Intake Guide')),
      body: const Center(
        child: Text('Sugar & Oil Intake Conversion Guide - Coming Soon!'),
      ),
    );
  }
}
