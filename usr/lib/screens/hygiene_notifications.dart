import 'package:flutter/material.dart';

class HygieneNotifications extends StatelessWidget {
  const HygieneNotifications({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Hourly Hygiene Notifications')),
      body: const Center(
        child: Text('Hourly Hygiene Notifications - Coming Soon!'),
      ),
    );
  }
}
