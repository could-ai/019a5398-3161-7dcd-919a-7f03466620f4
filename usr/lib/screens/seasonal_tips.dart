import 'package:flutter/material.dart';

class SeasonalTips extends StatelessWidget {
  const SeasonalTips({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Festival & Seasonal Health Tips')),
      body: const Center(
        child: Text('Festival & Seasonal Health Tips - Coming Soon!'),
      ),
    );
  }
}
