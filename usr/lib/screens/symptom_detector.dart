import 'package:flutter/material.dart';

class SymptomDetector extends StatelessWidget {
  const SymptomDetector({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Health Triage (AI Symptom Detector)')),
      body: const Center(
        child: Text('Health Triage (AI Symptom Detector) - Coming Soon!'),
      ),
    );
  }
}
