import 'package:flutter/material.dart';

class CustomAlarms extends StatelessWidget {
  const CustomAlarms({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Custom Alarm Setup')),
      body: const Center(
        child: Text('Custom Alarm Setup - Coming Soon!'),
      ),
    );
  }
}
