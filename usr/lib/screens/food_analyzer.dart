import 'package:flutter/material.dart';

class FoodAnalyzer extends StatelessWidget {
  const FoodAnalyzer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Food Analyzer')),
      body: const Center(
        child: Text('Food Analyzer - Coming Soon!'),
      ),
    );
  }
}
