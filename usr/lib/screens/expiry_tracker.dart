import 'package:flutter/material.dart';

class ExpiryTracker extends StatelessWidget {
  const ExpiryTracker({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Expiry Tracker')),
      body: const Center(
        child: Text('Expiry Tracker - Coming Soon!'),
      ),
    );
  }
}
